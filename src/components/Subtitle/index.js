import styled from 'styled-components'
import style from 'components/helpers/style'

const Subtitle = styled.h3`
  font-size: ${style.sizes.md};
  color: ${style.color.textColor};
  font-weight: 400;
`

export default Subtitle
